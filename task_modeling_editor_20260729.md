# 建筑建模编辑器实现 · 2026-07-29

## 目标
在 `indoor_map_geohub3d.html` 中添加建筑建模编辑器，支持用户直接在 AMap 地图上绘制商场室内布局，并通过「模式切换」与室内地图模式分开显示。

## 完成功能

### 1. 建筑建模编辑器面板（右侧浮层）
- **标题栏**：楼层下拉选择器 + 关闭按钮
- **工具栏**：选择、POI 标注、外墙绘制、隔墙绘制、矩形区域、多边形、撤销、清空
- **4 个标签页**：
  - 外墙/隔墙：列表管理，点击选中，右侧删除
  - 区域：矩形工具 + 颜色/透明度/标签设置
  - 标注点：POI 名称 + 5 种分类（购物/餐饮/娱乐/服务/停车）
  - 楼层：添加/删除/切换楼层
- **底部操作**：预览 3D · 保存项目 · 加载项目（JSON）

### 2. 画布绘制层
- 全屏透明 canvas 叠加在地图之上
- 鼠标点击绘制：外墙（多边形封闭）、隔墙（折线）、矩形区域、POI 标注
- 拖拽移动已绘元素
- 右键删除当前多边形顶点
- 键盘快捷键：Esc 取消当前绘制，Delete 删除选中，Ctrl+Z 撤销

### 3. 坐标转换
- `canvasToLngLat()` / `lngLatToCanvas()`：基于 AMap zoom 18 像素到经纬度转换
- 所有绘制数据以真实经纬度存储，支持 GeoJSON 导出

### 4. 模式切换
- **AMap 地图**：纯高德地图（indoor overlay 可用）
- **3D 建筑**：Three.js 3D 楼层模型 + POI 标注 + 导航路径
- **融合**：3D 叠加透明地图
- Header 模式按钮 + 地图右上角 modePill 均可触发切换
- 建模编辑器打开时自动关闭（进入 3D 预览时）

### 5. 3D 预览
- 点击「预览 3D」将编辑器数据应用到 Three.js 场景
- `edApplyToScene()`：将所有楼层的墙、区域、POI 转为 Three.js Mesh/Line
- 自动切换到 3D 建筑模式

### 6. 项目保存/加载
- 保存为 JSON 文件（含项目元数据 + 所有楼层数据）
- 加载时还原编辑器状态和绘制内容
- localStorage 备选（key: `indoor3d_edConfig`）

## 文件变更
- `indoor_map_geohub3d.html`：新增 ~500 行 CSS/HTML/JS
  - CSS：editor panel, toolbar, tabs, draw canvas 样式（约 200 行）
  - HTML：建模面板 DOM（约 150 行）
  - JS：编辑器全部逻辑 + `setMode` 合并（约 150 行）

## 测试结果
- ✅ 页面正常加载无报错
- ✅ 地图就绪状态正确
- ✅ 编辑器面板正常打开/关闭
- ✅ 楼层选择器工作正常
- ✅ 3D 建筑模式切换正常
- ✅ 截图确认 UI 正确渲染
